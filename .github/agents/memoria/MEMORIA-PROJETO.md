# Memoria de Projeto dos Agents

> Arquivo versionavel para registrar decisoes de demandas concretas do projeto em andamento.

## Regras de persistencia

- Todo agent deve ler este arquivo antes de atuar, em conjunto com `MEMORIA-COMPARTILHADA.md`.
- Este arquivo deve manter decisoes do projeto e da demanda atual: escopo, requisitos, arquitetura, implementacao, validacao, riscos, aceite e backlog de trabalho.
- Decisoes transversais sobre agents, skills, workflow, templates e governanca do pacote devem ser registradas em `MEMORIA-COMPARTILHADA.md` (memoria geral) e, quando relevante, referenciadas aqui com referencia cruzada.
- Quando houver solicitacao explicita para registrar em ambos os escopos, a mesma decisao deve ser persistida nas duas memorias com referencia cruzada.
- Detalhes extensos, cronologia de mudancas e evidencias completas devem ficar em `historico/`.

## Contexto do projeto atual

| Campo | Valor |
|---|---|
| Projeto | SCIE — Sistema de Cadastro e Inteligencia Eleitoral |
| Repositorio | `/home/sales/eleitor` |
| Branch principal | `develop` — toda PR e aberta para `develop` (base de integracao) |
| Foco atual | Cadastro eleitoral, lideranca, importacoes e analises territoriais (PostGIS) |
| Estado | Ativo |
| Responsavel de consolidacao | Tech Lead |
| Docs formais | `../../docs/` |

### Stack detectada no projeto

| Camada | Tecnologia |
|---|---|
| Backend | NestJS 11, TypeScript 5.9, PostgreSQL 16 + PostGIS, Node 20 |
| Frontend | React 19, Vite 8, TypeScript 5.9, Tailwind CSS v3, TanStack Router 1.170, TanStack Query 5.101, TanStack Form 1.33 |
| Auth | JWT local HS256, stateless — `POST /auth/login` |
| Testes E2E | Cypress 14 via Docker |
| Testes unitarios | Vitest 4 |
| i18n | i18next + react-i18next (locales: pt-BR, en) |
| Notificacoes | Sonner 2 (toast) |

### Convencoes criticas do projeto

| Assunto | Convencao |
|---|---|
| Testes | Sempre via Docker — nunca instalar Cypress localmente |
| Hot reload backend | `docker restart scie-backend` apos alteracoes (sem watch) |
| Envelope API | `{ success, data, meta, error }` — `ApiClient.request()` faz unwrap automatico |
| Perfis | `admin_nacional` \| `coordenador_regional` (minusculo com underscore) |
| Commits | Conventional Commits + Gitflow |
| Pull Requests | **Sempre abertas para `develop`** (branch base de integracao), nunca para `master` |
| Swagger | Todo novo endpoint precisa de decoradores completos + tag em `main.ts` |
| Validacao de formularios | zod + helper `src/lib/validation/zod-errors.ts` (`zodErrors(schema, values)`) no submit |

## Decisoes ativas de projeto

| ID | Decisao | Impacto no projeto | Dono | Status |
|---|---|---|---|---|
| PRJ-DEC-01 | CPF de liderança e unico global — bloqueia recadastro mesmo apos desativacao. **Atualizado 2026-06-19 (PRJ-DEC-13):** passa a existir **fluxo de reativacao** (volta status a 'active' sem recriar; CPF/e-mail unicos seguem validos). | Conflito 23505 em `uq_liderancas_cpf_hash` tratado como erro tipado `cpf_already_in_use` (400); reativacao via `PATCH /liderancas/:id/reativar`. | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-02 | E-mail de liderança e opcional e unico entre lideranças, presente no cadastro e na edicao. | Migration 023 + `uq_liderancas_email`; tradutor `email_already_in_use` (400). | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-03 | Obrigatorios do cadastro de liderança: nome, CPF e telefone; demais campos opcionais (coordenador opcional). | Telefone obrigatorio no frontend (zod+required) e no backend (`Leadership.create`); edicao mantem assimetria. | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-04 | Validacao de formularios padronizada com zod via helper compartilhado `zodErrors`, sem migrar para o adapter nativo do TanStack. | Convencao firmada para todo novo formulario; mensagens via `t(...)`. | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-05 | Import de locais de votacao e assincrono: `POST` retorna 202 `{ jobId }` + polling; resolve 504 definitivamente. | Migration 027 `voting_location_import_jobs`; job in-process (risco de orfao em restart — follow-up fila externa). | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-06 | Cards do dashboard passam a ter acesso por perfil, configurado na pagina de Permissoes de Menu (mesmo modelo dos menus). Visibilidade final = permitido pelo perfil E nao ocultado pela preferencia por-usuario (`hiddenCards`). `admin_nacional` sempre ve todos. | Frontend-only. Nova API em `menu-permissions.ts` (chave `scie.dashboard.card.permissions`, reusa evento `scie:menu-permissions-updated`); hook `useDashboardCardPermissions`; filtro em `AdminDashboard`. Vitest 86/86; tsc OK. E2E (Cypress) e aprovacao do solicitante pendentes (handoff QA). | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-07 | Card "Escopo de metas de votos" no dashboard: total = soma das metas por municipio (`voting_goals.targetVotes`), comparado a soma das metas (votos esperados) por coordenador e por lideranca, com indicador de cobertura (alocado/total). Coordenador: `voteExpectationTotal` no modo `total`, senao soma `coordinatedMunicipalities[].expectedVotes`; lideranca: `voteExpectation` se >0, senao soma `activityMunicipalities[].expectedVotes` (evita dupla contagem). | Frontend-only, reusa list use-cases (escopo de acesso herdado do backend). Func pura `lib/voting-goal-scope.ts` (+spec), hook `useVotingGoalScope`, card `VotingGoalScopeCard`; registrado como card `votingGoalScope` (permissao por perfil PRJ-DEC-06). Frontend 97/97; tsc OK. **E2E Cypress 4/4 (Docker, backend real)** — `cypress/e2e/dashboard/voting-goal-scope.cy.ts` (admin + coordenador; invariante alocado=coord+lideranças e cobertura derivada). Aprovacao do solicitante pendente (DEC-STR-07). | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-11 | Integração **Google Calendar** para nova **Agenda de compromissos** (`Appointment`), conta **única** da aplicação que cria eventos e convida participantes (`attendees`+`sendUpdates`); escopo `calendar.events`. Entrega **incremental**: Inc1 (domínio/use cases/backoff/migration) **feito**; Inc2 (infra googleapis+Postgres+crypto+controllers CRUD) **feito**; Inc3 (OAuth connect+webhook+cron de renovação) **feito**; Inc4 (frontend) **feito**. **Bloqueante:** credenciais GCP (Client ID/Secret/redirect) + domínio HTTPS público p/ webhook — via env vars, documentado. | **Inc1 (commitado):** domínio `Appointment` (+spec), util puro `truncatedExponentialBackoff`/`retryWithBackoff` (+spec), ports (`AppointmentRepositoryPort`/`GoogleCalendarPort`/`GoogleCalendarAccountPort`), use cases Create/Update/Delete/List (testados com fake do port), in-memory repo, migration **028** (`appointments`/`appointment_participants`/`google_calendar_account`) nos 2 runners. **Inc2/3 (commitado):** repos Postgres (`postgres-appointment`, `postgres-google-calendar-account` com cripto AES reusada), `GoogleCalendarService` (googleapis, `sendUpdates=all`, `quotaUser`, backoff), controllers `agenda` (CRUD), `google` (auth-url/oauth2callback/status), `webhook/calendar`, `WatchRenewalService` (@Cron 6h + jitter), wiring no AppModule (+ScheduleModule), deps `googleapis`+`@nestjs/schedule@^6`, `.env.example`. Boot OK; `GET /agenda`→[], `/google/status`→not configured, webhook→200; tsc EXIT 0; backend unit **402/402**. **Inc4 (commitado):** seção `agenda` (rota/menu/permissão), tipos+repo+use cases+hook `useAppointments`, `AppointmentManagementPanel` (form+participantes+conectar Google+lista/cancelar), i18n pt-BR/en; frontend tsc EXIT 0, unit **109/109**, **E2E agenda 3/3** (Docker, backend real). **Fix de roteamento (2026-06-15):** as rotas novas (`/agenda`, `/google`, `/webhook`) **não** estavam no proxy do nginx (prod) nem do Vite (dev) → cairiam no fallback da SPA; adicionadas em `frontend/nginx.conf` e `vite.config.ts`. Env vars do Google: `env_file` opcional em `docker-compose.yml` (dev, `backend/.env` gitignorado) e `${GOOGLE_*}` no `docker-stack.yml` (prod, secret store). E2E agenda endurecido (4/4) para exigir JSON do backend (pega regressão de proxy). **Pendente:** credenciais são de host público (deploy) → verificação real ponta-a-ponta é no ambiente implantado (handoff). `GOOGLE_REDIRECT_URI` deve ser `https://<host>/google/oauth2callback` (== registrado no GCP). System Design: `docs/system-design/2026-06-15-google-calendar-integration.md`. Branch `feature/google-calendar-integration`. | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-10 | **Fix (backend):** `PostgresRegionalCoordinatorRepository.listAll()` trazia só os NOMES dos municípios (`ARRAY(municipality::text)`), mapeando `state:null, expectedVotes:null` → a expectativa de votos por município nunca chegava pela LISTAGEM (só no detalhe via `loadMunicipalities`). Trocado por subquery `json_agg(id,state,municipality,expected_votes)` (sem N+1; bairros seguem `[]` na lista, sem regressão). **Fix (frontend):** `computeMunicipalityGoalBreakdown` deixou de filtrar por `voteExpectationMode` — soma `expectedVotes` por município em QUALQUER modo (projeto permite votos por município no modo `total`, ajuste de 2026-06-14). Sintoma: card exibia 0 para Bujari embora o coordenador (modo `total`) tivesse 100 registrado. | Verificado no endpoint real: `GET /coordenadores-regionais?all=true` retorna Bujari `expectedVotes:100, state:AC`. Lideranças não afetadas (`loadActivityMunicipalitiesForMany` já trazia `expected_votes`). Backend reiniciado (`docker restart scie-backend`). Frontend 109/109; tsc EXIT 0; E2E 6/6. Teste real do repo Postgres (Testcontainers/DinD) em handoff QA. | Senior Developer | Ativa |
| PRJ-DEC-09 | Card "Metas por município" (gráfico) no dashboard, **lado a lado** com o card de total (`VotingGoalScopeCard`). Refinado: **lista a UNIÃO dos municípios com meta (`voting_goals`) OU com dados de coordenador OU liderança** (presença em `coordinatedMunicipalities`/`activityMunicipalities`, inclusive sem votos), comparando por município **meta do município** (`voting_goals.targetVotes`) **× alocado** (soma dos votos esperados de coordenadores + lideranças). Ordenado por `max(meta, alocado)` desc. Votos por município somados em QUALQUER `voteExpectationMode` (ver PRJ-DEC-10); UF de entrada sem UF resolvida pelo nome via metas. | Frontend-only. Func puras `computeGoalsByMunicipality` + `computeMunicipalityGoalBreakdown` em `lib/voting-goal-scope.ts` (+spec); hook `useVotingGoalsByMunicipality` compoe metas+coordenadores+liderancas (reusa queries → dedupe); card `VotingGoalByMunicipalityCard` (2 barras Meta×Alocado por município, legenda, lista scrollavel de todos, tooltip coord/lideranças); card id `votingGoalByMunicipality` (permissao por perfil). Grid 2-col (lg). Tambem corrigido narrowing em `useDashboardCardPermissions.ts`. Frontend 109/109; tsc EXIT 0; E2E Cypress 6/6 (Docker, backend real). Aprovacao do solicitante pendente (DEC-STR-07). | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-12 | Mapa Espacial usa **clustering server-side (PostGIS) por viewport+zoom**, não listagem total. Tentativa de `all=true` foi reprovada (4,5M locais; ~100.891 por eleição+UF em SP → payload ~57 MB → navegador travado). A lista do painel **segue a área visível**: pontos individuais até `MAP_POINT_THRESHOLD=800` na viewport; acima disso, clusters agregados por grade. | `GET /locais-de-votacao/mapa/clusters` (bbox+zoom). Capacidade `all` mantida no backend/repo como recurso reutilizável. SP inteiro → 7 clusters/566 bytes/0,42 s. E2E mapa 2/2. Follow-up DBA: coluna `geometry` + GiST se a agregação por bbox crescer. | Solicitante / Senior Developer | Ativa |
| PRJ-DEC-08 | Fix do healthcheck do `frontend` em `docker-compose.yml` (dev): `wget` passou a usar `127.0.0.1:5173` em vez de `localhost:5173`. No container alpine `localhost` resolve para IPv6 `::1`, mas o Vite escuta so em IPv4 `0.0.0.0` → container ficava eternamente `unhealthy`, bloqueando o gate `depends_on: service_healthy` do cypress. | `scie-frontend` agora fica `healthy`; cypress roda sem `--no-deps`. `docker-stack.yml` (prod/nginx, porta 80) ja usava `127.0.0.1` — nao afetado. | Senior Developer | Ativa |

| PRJ-DEC-13 | **admin_nacional: ver registros desativados + exclusão real (hard delete) condicionada a "não associado a outros registros".** Decisões do solicitante (2026-06-19): escopo = todos os cadastros (lideranças, coordenadores, eleitores, candidatos/locais/eleições); fluxo = **hard delete direto** (botão na listagem, sem exigir desativação prévia); com dependência = **bloquear (409) e explicar** os vínculos. Para coordenador, bloqueiam: **lideranças vinculadas + coordenadores subordinados** (NÃO portfólios de eleitores, NÃO atribuições de região/locais); o **login interno é excluído junto** (vínculo indissolúvel). **Eleitor adiado** (não incluir hard delete agora — manter só status visível). | **Fase 1 — Lideranças ENTREGUE (padrão canônico):** backend — `listAll/listByCoordinatorId(includeInactive)`, `hardDelete`, `HardDeleteLeadershipUseCase` (admin_nacional), `GET /liderancas` inclui desativados só p/ admin, novo `DELETE /liderancas/:id/permanente` (sem dependentes — nenhuma tabela referencia `liderancas`). Frontend — coluna Status (badge), botão "Excluir" admin-only, modal irreversível, repo/use-case/hook/Services wiring; i18n pt-BR/en. **Testes:** backend unit 338 verdes (coordination 80/80, hard-delete 4/4); frontend **121/121** (+3); `tsc` EXIT 0 nas 2 pontas. **Specs de bootstrap de módulo falham SÓ localmente** por `@nestjs/schedule`/`googleapis` ausentes (presentes no container) — handoff QA roda no Docker. **Fase 2 — Coordenadores BLOQUEADA p/ DBA gate:** hard delete do `internal_users` é RESTRICTed por múltiplas FKs de auditoria (`assigned_by`/`defined_by`/`published_by`/`changed_by`/`registered_by`) + `voter_portfolios`(RESTRICT) + `coordinator_hierarchy.superior_id`(RESTRICT). Excluir o login exige nulificar referências de auditoria (sensível a LGPD) → exige parecer do DBA antes de implementar. **Fase 3 — Candidatos/Locais/Eleições ENTREGUE:** mapeamento de FK mostrou que **candidatos e locais NÃO têm dependentes** (nenhuma tabela os referencia) → o hard delete admin-only já existente satisfaz "excluir se não associado"; **sem mudança**. Só **eleições** têm dependentes (todos CASCADE). Como a edição de eleição é `delete+recreate` (acoplada ao `DELETE /pleitos/:id` cascata), NÃO se alterou o delete existente; criou-se endpoint separado **`DELETE /pleitos/:id/permanente`** (`HardDeleteElectionUseCase`, admin-only) que **bloqueia 409** quando há candidatos/locais/resultados/metas/sessões, com `countDependents` no repo. Frontend: a ação "Excluir" da listagem usa o novo caminho com bloqueio (`onHardRemove`), edição segue cascata. **Testes:** `hard-delete-election` 4/4, backend unit **342** verdes; frontend **122/122** (+1 election); `tsc` EXIT 0. **Fase 2 — Coordenadores ENTREGUE (Opção A aprovada pelo solicitante):** parecer `docs/dba/2026-06-19-hard-delete-coordenador.md`. Backend: `countHardDeleteBlockers` + `hardDelete` transacional no repo Postgres (lock+recheck anti-TOCTOU; apaga `voter_portfolios` sem tocar eleitores; nulifica `previous_coordinator_id` e as 8 colunas `*_by` de auditoria; apaga coordenador→`internal_users`), `CoordinatorHasDependentsError`, `HardDeleteRegionalCoordinatorUseCase` (admin_nacional, 409 lideranças+subordinados, **registra `regional_coordinator_hard_deleted` em `administrative_audit_logs`**), `DELETE /coordenadores-regionais/:id/permanente`, listagem inclui desativados p/ admin (`includeInactive`). Frontend: coluna Status, botão "Excluir" admin-only, modal irreversível, 409 via toast; repo/use-case/hook/page/i18n. **Testes:** `hard-delete-regional-coordinator` 5/5, coordination 85/85, backend unit **347**; frontend **122/122**; `tsc` EXIT 0 nas 2 pontas. **PR #146** (atualizado). Migrations opcionais (C1→RESTRICT, `*_by`→SET NULL) ficam como follow-up. **Eleitores seguem adiados.** **Refinamento 2026-06-19 (prompt 002):** ações por linha condicionadas ao status para admin_nacional — **desativado → [Excluir, Reativar]**, **ativo → [Editar, Remover]** (o botão Excluir deixou de ser sempre visível). Novo **fluxo de reativação**: `PATCH /liderancas/:id/reativar` (`ReactivateLeadershipUseCase`) e `PATCH /coordenadores-regionais/:id/reativar` (`ReactivateRegionalCoordinatorUseCase` reativa coordenador + `internal_users`, audit `regional_coordinator_reactivated`); `InternalUserRegistryPort.reactivate` + repos. Eleições/candidatos/locais não têm estado desativado → mantêm ação única. Testes: backend unit **353** (reactivate lid 3/3 + coord 3/3), frontend **124/124** (panel 21/21), `tsc` EXIT 0. Atualiza PRJ-DEC-01. **Caveat ambiente:** `@nestjs/schedule`/`googleapis` (de PRJ-DEC-11) declarados mas NÃO instalados no node_modules deste ambiente → specs que dão bootstrap em `app.module` falham só aqui; rodam no Docker do QA. Branch `feature/admin-hard-delete-cadastros` (de develop). Log `docs/prompts/2026-06-19_001_*`. **Fixes pós-merge (2026-06-20):** PR #148 (spec de soft-delete do coordenador realinhado à nova visibilidade de desativados) e PR #150 (build TS2820 — ações `regional_coordinator_hard_deleted`/`reactivated` adicionadas ao union `AdministrativeAuditAction`). | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-14 | **Envio de e-mail via Gmail API, config/credenciais por env** (decisões 2026-06-20): transporte **Gmail API (OAuth2)** reusando `googleapis`; credenciais por env (`GOOGLE_CLIENT_ID`/`SECRET` + `GMAIL_REFRESH_TOKEN` escopo `gmail.send` + `GMAIL_SENDER`/`GMAIL_SENDER_NAME`); escopo = **só o serviço** (sem plugar reset de senha). | `MailerPort` (`application/interfaces/notification`) + token `MAILER_PORT`; builder puro `infrastructure/email/mime-message.ts` (RFC822/2047, multipart/alternative, base64url); adapter `GmailMailerService` (`gmail.users.messages.send`, degrada sem env → `MailerNotConfiguredError`); controller admin `email` — `GET /email/status`, `POST /email/test` (`@RequireProfiles('admin_nacional')`, 503 se não configurado), tag em `main.ts`; wiring `app.module` (`useExisting`); `.env.example` (substitui bloco `MAIL_*` morto). **Testes 21** (mime 8, service 7 com mock googleapis, controller 6); `tsc` limpo (único erro local = `googleapis` ausente, env gap). Consumidores reais (ex.: reset de senha) ficam para depois injetando `MAILER_PORT`. Verificação de envio real = ambiente implantado (handoff). Branch `feature/gmail-mailer`. Log `docs/prompts/2026-06-20_001_*`; registro `docs/reviews/2026-06-20-1230-*`. **Fix de prod (2026-06-21, PR #163):** a stack `docker-stack.yml` repassava só `GOOGLE_*` ao backend e não tem `env_file` → `GMAIL_REFRESH_TOKEN`/`GMAIL_SENDER`/`GMAIL_SENDER_NAME` não chegavam ao container (mailer `configured:false` em prod). Adicionadas as 3 `GMAIL_*` ao `environment:` do backend (padrão `${VAR:-}`; segredos no Portainer). Dev (`docker-compose.yml`) já cobria via `env_file: backend/.env`. Log `docs/prompts/2026-06-21_001_*`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-15 | **Remoção de 4 cards do dashboard** (decisão do solicitante 2026-06-20): **Eleitores** (`voters`), **Regionais no recorte** (`regionalsInScope`), **Perfil da sessão** (`sessionProfile`) e **Hierarquia de Coordenadores** (`coordinatorTree`). Remoção **completa** (não ocultação): saem do render, do catálogo, da página de Permissões de Menu e do modal de preferências. | Frontend-only. `dashboard-cards.ts` (tipo+catálogo), `AdminDashboard.tsx` (KPIs + bloco `CoordinatorHierarchyTree` + imports/derivados órfãos), `menu-permissions.ts` (defaults por perfil), specs `useDashboardCardPermissions`/`menu-permissions` (ids representativos `voters`/`coordinatorTree`→`pending`/`votingGoalScope`), i18n pt-BR/en (`dashboardCards.cards.*` órfãos). Mantém `CoordinatorParticipationCard` (fora do escopo); componente `CoordinatorHierarchyTree` preservado em arquivo. `tsc` EXIT 0; ESLint EXIT 0. Log: `docs/prompts/2026-06-20_002_remover-cards-dashboard.md`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-16 | **Novo card "Metas por município no mapa"** (decisão do solicitante 2026-06-20): mapa que mostra, por município, a **meta do município** (`voting_goals.targetVotes`) e as metas de **coordenadores** e **lideranças**, com **filtro por UF** e geo de **API online (IBGE)**. Decisões (AskUserQuestion): **polígonos + marcador no centróide**; **uma UF por vez** (padrão = UF de maior meta); **métrica primária = cobertura** (raio ∝ meta, cor = alocado÷meta). | Frontend-only. Reusa `computeMunicipalityGoalBreakdown`/`useVotingGoalsByMunicipality` (dedupado). Geo IBGE sem credencial: v1 localidades (`nome↔código`) + v3 malhas (`/api/v3/malhas/estados/{UF}?intrarregiao=municipio&...geo+json`, `codarea`); casamento por **nome normalizado**. Novos: `lib/voting-goal-map.ts` (puro+spec), `hooks/useMunicipalityGeometries.ts` (react-query, cache 24h), `components/dashboard/VotingGoalMapCard.tsx` (react-leaflet: GeoJSON polígonos + CircleMarker + Popup + legenda; +spec, react-leaflet stubado). Wiring: card id `votingGoalMap` (catálogo + permissões admin_regional/coordenador_regional; admin_nacional via ALL_CARDS) + render full-width no `AdminDashboard` + i18n pt-BR/en. **Refinamentos:** filtro lista **todas as 27 UFs** (`BRAZIL_STATES`; UF sem dados avisa e não busca geometria); marcador com **rótulo permanente** `meta / alocado` (alocado = coord+lideranças), popup mantém detalhamento. **Validação:** `tsc` EXIT 0, Vitest **148/148**, ESLint EXIT 0 (container `scie-frontend`; `--no-maglev --no-turbofan` p/ crash V8 turboshaft documentado). Endpoints IBGE verificados manualmente. Logs: `docs/prompts/2026-06-20_003_*`, `_004_*`, `_005_*`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-17 | **Integrações Google (Calendar) + e-mail (Gmail) 100% por variáveis de ambiente, sem fluxo OAuth interativo; UI só de status+teste** (decisões 2026-06-21): (a) **remover toda a feature Agenda de compromissos** (CRUD/UI/tabelas/OAuth-connect/webhook/cron); (b) segredos de **dev direto no `docker-compose.yml`**; (c) teste do Calendar = **criar e apagar evento**. | Calendar autentica como o mailer: `GOOGLE_CLIENT_ID/SECRET` + `GOOGLE_CALENDAR_REFRESH_TOKEN` + `GOOGLE_CALENDAR_ID`. **Backend:** removidos domínio/use-cases/ports/repos de `appointment`, controllers `/agenda`,`/google`,`/webhook/calendar`, `WatchRenewalService`+`ScheduleModule`; migration **029** dropa as 3 tabelas (2 runners); `GoogleCalendarService` → env + `isConfigured()` + `testConnection()`; novo `CalendarController` (`GET /calendar/status`, `POST /calendar/test`, admin_nacional, padrão do `EmailController`). **Frontend:** removida feature Agenda; novo modal admin `IntegrationsSettings` (menu "Integrações") com cards Calendar+E-mail (status badge + botão Testar; e-mail com campo `to`); `useIntegrations`+repo+use-cases+i18n; proxies nginx/vite trocados (`/calendar`,`/email`). **Infra:** `docker-compose.yml` recebe as 7 vars in-place; `docker-stack.yml` reescrito (remove OAuth-flow/webhook, add `GOOGLE_CALENDAR_REFRESH_TOKEN`+`GMAIL_*`) — **supera PR #163**. **Validação:** backend `tsc` 0 + Vitest **443**; frontend `tsc` 0 + Vitest **155** + ESLint limpo (resta 1 `react-refresh/only-export-components` pré-existente em `ServicesProvider`). Execução: Tech Lead + 2 subagents (backend/frontend) em paralelo. **Pendente:** verificação funcional real (credenciais) + E2E (handoff QA); fechar/realinhar PR #163. Log: `docs/prompts/2026-06-21_002_*`. | Solicitante / Tech Lead | Ativa |

| PRJ-DEC-18 | **Gerar `refresh_token` do Google pela UI** (atalho de setup, decisão 2026-06-21): botão "Gerar token" no painel Integrações dispara o fluxo OAuth e o backend **exibe** o token numa página HTML para o operador colar no env. **Mantém env-only** (PRJ-DEC-17): o token **não é persistido**. | Backend: `GoogleOAuthTokenService` + `IntegrationsOAuthController` — `GET /integrations/oauth/start?target=calendar\|gmail\|both` (admin_nacional, `{url}`, 503 se não configurado; `state`=JWT `purpose:'oauth-token-gen'` 10min) e `GET /integrations/oauth/callback` (**público**, valida `state` antes de trocar o code, exibe o token em `<textarea readonly>` HTML-escapado + env vars por alvo, nunca persiste/loga; `@ApiExcludeEndpoint`). `jwt.service` `sign/verifyOAuthState`; novo env **`GOOGLE_OAUTH_REDIRECT_URI`** (registrar no Console). Frontend: botões `integration-{calendar,email}-generate` → `window.location` p/ a URL; proxy `/integrations` (vite+nginx). Infra: `GOOGLE_OAUTH_REDIRECT_URI` nos 2 compose; **corrigido `docker-stack.yml`** (removidas vars mortas OAUTH_SUCCESS_REDIRECT/WEBHOOK_*; add GOOGLE_CALENDAR_REFRESH_TOKEN). **Validação:** backend `tsc` 0 + Vitest **457**; frontend `tsc` 0 + Vitest **158** + ESLint limpo. Execução: Tech Lead + 2 subagents. **Pendente:** verificação real ponta-a-ponta + E2E (handoff QA). Log: `docs/prompts/2026-06-21_003_*`. | Solicitante / Tech Lead | Ativa |

| PRJ-DEC-19 | **Novo card "Votos dos candidatos de interesse por município (mapa)"** (decisão 2026-06-21): mapa que mostra a **soma dos votos nominais de TODOS os candidatos `is_of_interest=TRUE`** por município, com **detalhe por candidato no hover**; **seletor de eleição** (padrão = mais recente) + filtro por UF. | Backend `resultados-boletim`: `GET /resultados-boletim/candidatos-interesse/por-municipio?electionId&state` (JwtAuthGuard) — soma `ballot_results.votes` (`votable_type_code=1`) de candidatos de interesse por município (join `election_id+state+position_code+ballot_number`); port `sumVotesByInterestCandidatesPerMunicipality` + repo Postgres + use case + DTOs (tipos explícitos) + wiring. Frontend reusa `useMunicipalityGeometries`+`voting-goal-map.ts`; novos `lib/candidate-votes-map.ts`, hook `useCandidateVotesByMunicipality`, `CandidateVotesMapCard` (polígono+marcador, raio ∝ total, hover com breakdown), card id `candidateVotesMap` (permissões + render no `AdminDashboard`), i18n. **Validação:** backend `tsc` 0 + Vitest **464** + **boot OK** (Swagger sem ciclo); frontend `tsc` 0 + Vitest **173** + ESLint limpo. Execução: Tech Lead + 2 subagents. **Pendente:** teste Postgres real (DinD) + verificação com dados de urna + E2E (handoff QA). Log: `docs/prompts/2026-06-21_006_*`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-20 | **Autorização de escrita por perfil — lideranças e coordenadores** (decisões do solicitante 2026-06-22): (a) **lideranças** (`POST/PATCH /liderancas`) liberadas a **qualquer perfil autenticado** (menu `liderancas-listagem` aberto aos 5 perfis por padrão); (b) **coordenadores** (`POST/PATCH /coordenadores-regionais`) liberados a `admin_nacional`, `admin_regional` (Coordenador Geral), `coordenador_regional` e `analista_geoespacial` (Supervisor) — `registrador` sem escrita; (c) **anti-escalonamento**: o ator só atribui/gerencia coordenador cujo **perfil resultante** fique **estritamente abaixo** do seu. | Sem mudança de schema (perfis já existem). **Backend:** novo `domain/access/profile-hierarchy.ts` (`canAssignProfile`, rank admin_nacional5>admin_regional4>coordenador_regional3>analista_geoespacial2>registrador1); lideranças sem gate de perfil; coordenadores com `COORDINATOR_WRITER_PROFILES` + `canAssignProfile` (register: perfil selecionado; update: `f.profile ?? existing.userProfile ?? 'coordenador_regional'`). **Frontend:** `lib/profile-hierarchy.ts` (`assignableCoordinatorProfiles`/`canWriteCoordinators`); menu defaults (Supervisor ganha `coordenadores-listagem`; Supervisor+Registrador ganham `liderancas-listagem`); `CoordinatorManagementPanel` com `profileOptions` por rank + botão Novo/Editar condicionados a `canWriteCoordinators`. **Validação:** backend unit **475/475**, `tsc` 0, boot OK, HTTP real negativo 403 (registrador→coordenador; coordenador_regional→admin_regional; supervisor→coordenador_regional) e GET 200; frontend unit **182/182**, `tsc` 0. **Divergências:** ESLint pré-existente em `CoordinatorManagementPanel.tsx` (não introduzido); DELETE de coordenador segue admin-only (fora do escopo). **E2E Cypress (Docker, backend real) 5/5 verdes** — `coordinator-authorization.cy.ts` 3/3 (hierarquia só subperfis; coordenador_regional cadastra atribuindo Supervisor com `profile=analista_geoespacial`; registrador sem botão) + `leadership-authorization.cy.ts` 2/2 (Supervisor e Registrador cadastram liderança). Prereq: seed **005** (supervisor@/registrador@) + env Cypress; fix do perfil default em `registerDefaults`. **Caveat ambiente:** specs de coordenador dependem de `coord.norte@scie.local` (seed 002) — em dev DB com drift (hard delete) reaplicar 002 via reset de `schema_seeds`. **Caveat Cypress:** o serviço `cypress` tem `entrypoint: ["/bin/sh","-c"]` → passar o comando como **um único argumento string** (não prefixar `sh -c`, senão vira no-op com EXIT 0 enganoso). **Pendente:** aprovação do solicitante (DEC-STR-07). Log `docs/prompts/2026-06-22_001_*`; registro `docs/reviews/2026-06-22-1900-*`. PR **#182**. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-26 | **Andamentos/pendências de tarefas + card de pendências** (decisões do solicitante 2026-06-27): tarefa ganha **histórico de andamentos** (`task_progress`); cada andamento pode ser marcado como **pendência** com ciclo **aberta/resolvida**. Card do dashboard soma as pendências **abertas**. **Escrita** (andamento/resolver): admin_nacional/admin_regional em qualquer tarefa + coordenadores **associados** à tarefa (escopo/subárvore). **Listagem/soma**: só `admin_nacional` global; **demais perfis (inclusive admin_regional) escopados** às pendências de suas tarefas ou de coordenadores da **subárvore recursiva**. | **Migration 039** `task_progress` (`pendencia_status ∈ {none,aberta,resolvida}`, `registered_by`/`resolved_by`, índice parcial; 2 runners). Domínio `task-progress.ts`. `TaskRepositoryPort`: `addProgress`/`listProgressByTask`/`findProgressById`/`resolvePendencia`/`isTaskInActorScope`/`listOpenPendencias(scope?)`; escopo de tarefa via CTE recursiva reusando `coordinator-subtree.sql.ts` (PRJ-DEC-25): tarefa no escopo se coordenador associado ∈ subárvore OU área coordenada pelo ator. Use cases `AddTaskProgress`/`ListTaskProgress`/`ResolveTaskPendencia` (authz admin OU `isTaskInActorScope`) + `ListOpenPendencias` (admin_nacional global; demais escopado, `{items,total}`). Endpoints `GET /tarefas/pendencias`, `POST/GET /tarefas/:id/andamentos`, `PATCH /tarefas/andamentos/:progressId/resolver` (rota `pendencias` antes de `:id`). **Frontend** (Tech Lead + subagent): card `openPendencias` (catálogo+permissões+`AdminDashboard`, headline `total`+lista), seção "Andamentos" no `TaskFormModal` (listar/adicionar/resolver), hooks `useOpenPendencias`/`useTaskProgress`, repo+use-cases+`ServicesProvider`, i18n pt-BR/en, `data-cy`. Testes unit `task-progress.use-case.spec.ts` (authz + escopo + ciclo). **Divergência intencional:** admin_regional é escopado p/ pendências (≠ `GET /tarefas`, onde é global). **Depende de** PRJ-DEC-25/PR #230 (reusa `coordinator-subtree.sql.ts`). **Pendente:** build/test/boot no Docker do QA + E2E + aprovação (DEC-STR-07). Log `docs/prompts/2026-06-27_002_*`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-25 | **Escopo de listagem de coordenadores e lideranças por perfil** (decisões do solicitante 2026-06-27): `admin_nacional` e `admin_regional` listam **todos**; demais perfis (`coordenador_regional`, `analista_geoespacial`, `registrador`) listam apenas os cadastros que **criaram** (`registered_by = ator`) OU os de **coordenadores associados** (subárvore **recursiva** via `coordinator_hierarchy`, inclui o próprio). Legados (criador desconhecido) visíveis a não-admins só pela via estrutural (sem backfill). | **Bloqueio resolvido:** não havia coluna de criador → **migration 038** (`registered_by UUID` + índices em `regional_coordinators` e `liderancas`, 2 runners), preenchida no cadastro com o ator. `registeredBy` em `LeadershipRecord`(obrigatório)/`RegionalCoordinatorRecord`(opcional). Helper SQL `coordinator-subtree.sql.ts` (`WITH RECURSIVE`) usado em `listAll` (coord.) e `listAll`/`listByCoordinatorId` (lid.). Use cases recebem `actor` e decidem global×escopado via `hasGlobalListingAccess` (`GLOBAL_LISTING_PROFILES=[admin_nacional,admin_regional]`). Controllers repassam `accessScope.{internalUserId,profile}`. **Testes:** unit `list-leadership.use-case.spec.ts` (novo) + `list-regional-coordinators.use-case.spec.ts` + register asserts `registered_by`; integração `postgres-coordinator-leadership-scope.int.spec.ts` (novo, R→C1→C2 + isolado X). **Premissas/divergências:** desativados mantidos só p/ admin_nacional (confirmar admin_regional); endpoints de referência/dropdown seguem globais (fora do escopo); frontend sem mudança. **Handoff QA:** Docker indisponível no dev → `tsc`/unit/`*.int.spec.ts`/boot real no Docker do QA. **Pendente:** validação QA + aprovação do solicitante (DEC-STR-07). Branch sugerida `feature/listagem-escopo-por-perfil`. Log `docs/prompts/2026-06-27_001_*`. | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-24 | **Perfil próprio do usuário (edição de dados pessoais)** (2026-06-25): usuário autenticado pode visualizar e editar nome de exibição, e-mail (com senha atual), data de nascimento, nome do pai, nome da mãe e endereço. Alterar e-mail emite novo JWT (`newAccessToken`). | Migration 035 (`birth_date`/`father_name`/`mother_name`/`address` JSONB em `internal_users`). `GET /auth/me/profile` e `PATCH /auth/me/profile`. Frontend: `ProfileSettings` modal, `useMyProfile` hook, item "Meu perfil" no menu. E2E Cypress + aprovação do solicitante pendentes (DEC-STR-07). | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-23 | **Cadastro e gestão de Tarefas** (decisões do solicitante 2026-06-25): tarefa com `title`/`description`/`startDate`/`endDate`, associável a **vários coordenadores E/OU várias áreas** (N:N, ambos opcionais); gerida por **`admin_nacional` + `admin_regional`**; quando associada a uma área, cria **evento all-day na data de término** no calendário da área convidando **os coordenadores da área** (e-mail, `sendUpdates:'all'`), **best-effort**; **edição re-sincroniza** (move/atualiza/recria/apaga eventos), delete apaga eventos. | Migration **034** (`tasks`, `task_coordinators`, `task_areas` com `calendar_event_id`; 2 runners). Domínio `tasks/task.ts`; `TaskRepositoryPort` + `PostgresTaskRepository` (transacional; `getAreaCoordinatorEmails` via `coordinator_activity_areas→regional_coordinators→internal_users.email`); use cases create/update/delete/list/get reusando `GoogleCalendarPort` (all-day `end.date` exclusivo = endDate+1; best-effort). `GET /tarefas` etc. (`admin_nacional`+`admin_regional`); **`GET /areas-atuacao` relaxado p/ admin_regional** (CRUD de áreas segue admin_nacional). Frontend `TarefasPage` (menu `tarefas` + rota; multi-select coordenadores+áreas), `useTasks`, `useActivityAreas` habilitado p/ admin_regional. **Validação:** backend **549** + task specs **15** + `tsc` 0 + migration + boot + **round-trip real** (tarefa com área+coordenador → nomes resolvidos, `calendar_event_id` null em dev/best-effort, PATCH/DELETE ok); autz admin_regional 200/403 e coordenador_regional 403; frontend **218** + `tsc` 0 + ESLint limpo. **Pendente:** verificação real do calendário/e-mail (deploy, Google configurado) + E2E + aprovação (DEC-STR-07). Registro `docs/reviews/2026-06-25-0455-*`. Branch `feature/tarefas`. | Solicitante / Tech Lead | Ativa |
| **Nota QA (aprendizado)** | Erros de schema Swagger (DTO mal formado, ex.: `data:null` sem `type`) **só aparecem no boot** (`SwaggerModule.createDocument`), **não** nos testes unitários. Sempre validar com **boot real** (`docker restart scie-backend` + checar log `Nest application successfully started`) ao adicionar/alterar DTOs de resposta. Origem: hotfix PR #170 (2026-06-21). | — | Tech Lead | Ativa |
| PRJ-DEC-22 | **Áreas de atuação — gestão (CRUD), calendário e select** (decisões do solicitante 2026-06-24, evolui PRJ-DEC-21): (a) **tela de gestão** das áreas p/ `admin_nacional` (criar/listar/renomear/excluir) — substitui a criação inline; (b) ao **criar** a área, cria um **calendário Google** best-effort (Google off/erro → `calendar_id` NULL, área criada; recriável depois); (c) **exclusão = cascade** (remove área+alocações via FK + apaga calendário best-effort); (d) associação no coordenador vira **multi-select de áreas existentes** → contrato `activityAreaNames` → **`activityAreaIds`**; (e) **seed** de 9 áreas. | Migration **031** (`activity_areas.calendar_id`); seed **006** (Financeiro, ADM, Contábil, Agenda, CRM, Comunicações, Mobilização, Logística, Monitoramento; 2 runners). Catálogo CRUD + `CreateActivityAreaUseCase` (best-effort `GoogleCalendarPort.createCalendar`) + Rename/Delete (`deleteCalendar` best-effort) + `CreateAreaCalendarUseCase`; `ActivityAreaController` ganha POST/PATCH/DELETE + `POST /:id/calendario`. `replaceActivityAreas` linka por **id** (FK; sem find-or-create). Frontend: `ActivityAreasPage` (admin, menu `areas-atuacao` + rota) + multi-select de ids no painel do coordenador. **Validação:** backend coordination **104/104** + `tsc` 0 + migration/seed + boot + **round-trip ids real** (`activityAreaIds:[Financeiro,CRM]`→read-back ok; POST 201 calendarId null dev; dup 409; DELETE 404 repetido); frontend `tsc` 0 + ESLint limpo. Flakes pré-existentes (TOTP/Leadership) passam isolados. Renome não sincroniza summary do calendário (port sem `calendars.patch`). **E2E Cypress 5/5** (gestão CRUD admin 3/3 + alocação multi-select/admin-only 2/2). **Bug achado pelo E2E:** `/areas-atuacao` faltava no proxy dev (vite) e prod (nginx) → SPA fallback; adicionado nos dois. Registro `docs/reviews/2026-06-24-1810-*`. PR **#196** (rebased sobre develop pós-#193/#195). | Solicitante / Tech Lead | Ativa |
| PRJ-DEC-21 | **Áreas de atuação para alocação de coordenadores** (decisões do solicitante 2026-06-24): (a) área = **entidade nomeada (catálogo)** própria; (b) gerida **inline no formulário do coordenador** (find-or-create por nome, sem tela separada) — **superado por PRJ-DEC-22**; (c) **somente `admin_nacional`** gerencia/aloca; alocação **N:N opcional**. | Migration **030** (`activity_areas` com `name_normalized` UNIQUE; `coordinator_activity_areas` N:N FK CASCADE; nos 2 runners). API: `GET /areas-atuacao` (admin) lista catálogo; register (`coordinator.activityAreaNames[]`) e update (`activityAreaNames[]`) find-or-create + **substituem** alocações **só** para `admin_nacional` (gate independente — segue correto após PRJ-DEC-20 mesclar); leitura expõe `activityAreas:{id,name}[]`. Repo `replaceActivityAreas`/`loadActivityAreas` (transacional + json_agg, espelha municípios). Frontend: seção "Áreas de atuação" **admin-only** (chips + `<datalist>`; prefill no editar) + `useActivityAreas` (react-query). **Validação:** backend **475/475** + `tsc` 0 + migration aplicada + boot OK + **round-trip HTTP real** (PATCH dedupe normalizado → 2 áreas; 401/403 não-admin); frontend **177/177** + `tsc` 0 + ESLint limpo nos novos. Execução: Tech Lead + 2 subagents. **Pendente:** E2E Cypress + aprovação do solicitante (DEC-STR-07). Log `docs/prompts/2026-06-24_001_*`; registro `docs/reviews/2026-06-24-1630-*`. Branch `feature/coordenador-areas-atuacao`. | Solicitante / Tech Lead | Ativa |
| **Nota QA (aprendizado)** | `<input type="number">` focado altera o valor em ±step ao rolar o mouse. Em formulários roláveis isso causava redução esporádica de **1** na expectativa de votos (apoiadores/lideranças). Fix central no `TextBox`: `onWheel` desfoca inputs numéricos. Ao criar campos numéricos, garantir esse comportamento. Origem: PR #191 (2026-06-22). | — | Tech Lead | Ativa |

| PRJ-DEC-21 | **Gestão completa de calendários Google** (decisão 2026-06-22): ler/gerenciar os calendários da conta registrada — **criar/excluir calendário**, **CRUD de eventos** (com convidados+convite e recorrência) e **compartilhar (ACL por e-mail+papel)**. Reintroduz/expande a UI removida no PRJ-DEC-17, mantendo auth 100% por env. | **Escopo OAuth ampliado** para `https://www.googleapis.com/auth/calendar` (total) — `CALENDAR_SCOPE`/`GOOGLE_CALENDAR_SCOPE`, CLI `google-refresh-token.mjs`, `.env.example`, `scripts/README.md`; **operador regenera o token**. **Backend:** `GoogleCalendarPort`/`Service` expandidos (list/create/delete calendars; events CRUD com sendUpdates+recurrence; list/share/unshare ACL), `CalendarManagementController` (`/calendar`, admin_nacional+admin_regional, 10 rotas; 503 não-config, `CalendarOperationError`→400). **Frontend:** seção `calendarios` (`/app/calendarios`, menu standalone, permissão admin_nacional/admin_regional), `CalendariosPage`+`CalendarManagementPanel`+`EventFormModal`(zod)+`ShareDialog`, repo+10 use-cases+hooks+i18n. **Validação:** backend `tsc` 0 + Vitest **501** + **boot OK** (10 rotas, Swagger sem ciclo); frontend `tsc` 0 + Vitest **192** + ESLint limpo. Execução: Tech Lead + 2 subagents. **Pendente:** regenerar token (escopo total) + verificação real + E2E (handoff QA). Log: `docs/prompts/2026-06-22_004_*`. | Solicitante / Tech Lead | Ativa |

## Backlog de projeto

| Item | Estado |
|---|---|
| **Coordenador — hard delete (Fase 2)** | **Bloqueado — DBA gate.** `internal_users` RESTRICTed por FKs de auditoria `*_by` (LGPD). Necessário parecer DBA sobre estratégia (nulificar auditoria vs. impedir). Bloqueio + pré-checagem de lideranças/subordinados já decididos (PRJ-DEC-13). |
| **Eleitor — hard delete** | Adiado por decisão do solicitante (Fase posterior). Manter apenas status visível. |
| **Candidatos/Locais/Eleições — padronizar bloqueio por dependência (Fase 3)** | Pendente. Hoje cascateiam; trocar para 409+explicação. Reusa o padrão de `HardDelete*UseCase`. |
| **E2E Cypress (Docker) + aprovação do solicitante (DEC-STR-07)** | Pendente para todas as fases entregues, começando por Lideranças. |
| Suites de integracao (`*.int.spec.ts`) no container runtime | Bloqueado — Testcontainers exige Docker-in-Docker (handoff QA) |
| Otimizacao da query de densidade PostGIS (superlinear, ~6s @ 2000 eleitores) | Pendente — recomendacao do achado de capacidade |
| Refatorar `/files` (BYTEA em memoria, risco OOM em uploads de 800 MB) | Follow-up — mover para disco/object storage |
| Recuperacao de jobs de import orfaos apos restart do backend | Follow-up — single replica |
| Rollout de thresholds de cobertura (O6) conforme a suite cresce | Pendente |
| Mapa Espacial — coluna `geometry`+GiST p/ agregação por bbox | **Avaliado + BENCHMARK medido (DBA, 2026-06-17): não fazer agora; e quando crescer, usar índice cobertor, NÃO geometry+GiST.** Benchmark (índices em txn+ROLLBACK): cobertor `(election_id,state,longitude,latitude)` → statewide index-only (Heap Fetches 0, 12× menos buffers, ~100 ms) e mid-zoom range scan por longitude (39,8 ms, 2,3×); geometry+GiST (`btree_gist`) medido **inferior** (63,9 ms, 33k buffers por heap fetch) e exige reescrita de tabela. GiST só sob mapa livre multi-UF/raio. Gatilhos: p95>400 ms ou cardinalidade `(election_id,state)`>300k. **Script pronto (não aplicado):** `backend/scripts/optional/mapa-clusters-covering-index.{up,down}.sql` + README (não é migration: runner usa BEGIN/COMMIT e `CREATE INDEX CONCURRENTLY` não roda em txn; aplicar só por gatilho). Parecer: `docs/dba/2026-06-17-mapa-espacial-clustering-indices.md` |

## Aprovacoes do solicitante (aceite de QA)

| Data | Escopo aprovado | Observacao |
|---|---|---|
| 2026-06-20 | Revisão de cards do dashboard (PRJ-DEC-15: remoção de Eleitores/Regionais/Perfil da sessão/Hierarquia de Coordenadores; PRJ-DEC-16: novo card "Metas por município no mapa" com filtro por UF e rótulo meta/alocado). `tsc` EXIT 0; Vitest **148/148**; ESLint EXIT 0 (container `scie-frontend`). | "all approved" — aprovação explícita do solicitante. E2E Cypress ainda não criado (handoff QA). **PR #156** (`feature/dashboard-cards-revisao-mapa-metas` → `develop`, labels `in-review`+`feature`). Reaprovacao exigida para alteracoes posteriores (DEC-STR-07). |
| 2026-06-15 | Validacao zod nos formularios, telefone obrigatorio no cadastro de liderança, regressao E2E de lideranças (3/3 Cypress Docker), backend unit 378/378, frontend unit 72/72 | Sem ressalvas. PR #125. Reaprovacao exigida para alteracoes posteriores (DEC-STR-07). |
| 2026-06-17 | Mapa Espacial — clustering server-side (PostGIS) por viewport + lista que segue a area visivel. Backend unit 404/404; frontend unit 118/118; E2E Cypress mapa 2/2 (Docker, backend real); tsc EXIT 0 nas duas pontas. | Sem ressalvas. PR #144 (→ develop). Inclui revert do `all=true` (reprovado por payload ~57 MB/100k pontos). Reaprovacao exigida para alteracoes posteriores (DEC-STR-07). |

## Nota de migracao

> O log cronologico detalhado das entregas do SCIE (entradas datadas de 2026-06-08 em diante) permanece, neste momento, em `MEMORIA-COMPARTILHADA.md`. A separacao das duas memorias foi adotada em 2026-06-15; a migracao desse historico de demanda para esta memoria de projeto e/ou para `historico/` e um passo de saneamento pendente, a ser executado sem perda de rastreabilidade.
